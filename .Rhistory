ags = clusterdata
d = dist(tags, method = "binary")
hc = hclust(d, method="ward")
plot(hc)
cluster.means = aggregate(tags,by=list(cutree(hc, k = 6)), mean)
set.seed(1680) # for reproducibility
library(dplyr) # for data cleaning
library(ISLR) # for college dataset
library(cluster) # for gower similarity and pam
library(Rtsne) # for t-SNE plot
library(ggplot2) # for visualization
library(klaR)
setwd("C:/Users/lukas/PycharmProjects/monimporter")
clusterdata <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
str(clusterdata)
head(clusterdata, n=10)
ags = clusterdata
d = dist(tags, method = "binary")
hc = hclust(d, method="ward")
plot(hc)
cluster.means = aggregate(tags,by=list(cutree(hc, k = 6)), mean)
ags = clusterdata
d = dist(tags, method = "binary")
hc = hclust(d, method="ward")
plot(hc)
cluster.means = aggregate(X,by=list(cutree(hc, k = 6)), mean)
cluster.means = aggregate(clusterdata$X,by=list(cutree(hc, k = 6)), mean)
ags = clusterdata
d = dist(tags, method = "binary")
hc = hclust(d, method="ward")
plot(hc)
cluster.means = aggregate(clusterdata$X,by=list(cutree(hc, k = 6)), mean)
d <- dist(clusterdata, method = "binary")
hc <- hclust(d)
plot(hc)
install.packages("cba")
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
library("cba")
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
pr <- proximus(x, max.radius=8, debug = TRUE)
summary(pr)
library("cba")
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
pr <- proximus(x, max.radius=8, debug = TRUE)
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
x <- as.logical(x))
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
x <- as.logical(x)
x <- as.logical(x$swagger)
x <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
D <- transform(x , swagger =as.logical(x$swagger))
D <- transform(D , info =as.logical(x$info))
D <- transform(D , host =as.logical(x$host))
D <- transform(D , basePath =as.logical(x$basePath))
D <- transform(D , schemes =as.logical(x$schemes))
D <- transform(D , consumes =as.logical(x$consumes))
D <- transform(D , produces =as.logical(x$produces))
D <- transform(D , paths =as.logical(x$paths))
D <- transform(D , definitions =as.logical(x$definitions))
D <- transform(D , parameters =as.logical(x$parameters))
D <- transform(D , responses =as.logical(x$responses))
D <- transform(D , securityDefinitions =as.logical(x$securityDefinitions))
D <- transform(D , security =as.logical(x$security))
D <- transform(D , tags =as.logical(x$tags))
D <- transform(D , externalDocs =as.logical(x$externalDocs))
write.csv(D , file = "kmodes structasbinary.csv", row.names = TRUE)
library("cba")
pr <- proximus(D , max.radius=8, debug = TRUE)
summary(pr)
library("cba")
pr <- proximus(D , max.radius=8, debug = TRUE)
summary(pr)
pr <- proximus(D[2:16] , max.radius=8, debug = TRUE)
summary(pr)
pr <- proximus(D[2:16] , max.radius=8, debug = TRUE)
summary(pr)
pr <- proximus(D[,2:16] , max.radius=8, debug = TRUE)
summary(pr)
D[2:16]
x <- D[2:16]
pr <- proximus(x , max.radius=8, debug = TRUE)
summary(pr)
set.seed(1680) # for reproducibility
library(dplyr) # for data cleaning
library(ISLR) # for college dataset
library(cluster) # for gower similarity and pam
library(Rtsne) # for t-SNE plot
library(ggplot2) # for visualization
library(klaR)
setwd("C:/Users/lukas/PycharmProjects/monimporter")
clusterdata <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
str(clusterdata)
heat <- clusterdata
row.names(heat) <- heat$X
heat <- heat[,2:16]
heat
heat_matrix <- data.matrix(heat)
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(5,10))
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(50,100))
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(50,100))
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(10,30))
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(10,30))
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(10,20))
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(5,10))
heat <- clusterdata
row.names(heat) <- heat$X
heat <- heat[,2:16]
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(5,10))
set.seed(1680) # for reproducibility
library(dplyr) # for data cleaning
library(ISLR) # for college dataset
library(cluster) # for gower similarity and pam
library(Rtsne) # for t-SNE plot
library(ggplot2) # for visualization
library(klaR)
setwd("C:/Users/lukas/PycharmProjects/monimporter")
clusterdata <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
str(clusterdata)
heat <- clusterdata
row.names(heat) <- heat$X
heat <- heat[,2:16]
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(5,10))
set.seed(1680) # for reproducibility
library(dplyr) # for data cleaning
library(ISLR) # for college dataset
library(cluster) # for gower similarity and pam
library(Rtsne) # for t-SNE plot
library(ggplot2) # for visualization
library(klaR)
setwd("C:/Users/lukas/PycharmProjects/monimporter")
clusterdata <- read.csv(file="clusterdfr.csv", header=TRUE, sep=",")
str(clusterdata)
heat <- clusterdata
row.names(heat) <- heat$X
heat <- heat[,2:16]
heat_matrix <- data.matrix(heat)
heat_matrix <- heatmap(heat_matrix, Rowv=NA, Colv=NA, col = cm.colors(256), scale="column", margins=c(5,10))
